spring:
  datasource:
    username: root
    password: kl709709
    url: jdbc:mysql://localhost:3306/shop
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace